apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: app1-route
  namespace: kube-system
spec:
  parentRefs:
  - name: web
    namespace: kube-system
    sectionName: http
  hostnames: ["app1.com"]
  rules:
  - backendRefs:
    - name: app-one
      port: 80
