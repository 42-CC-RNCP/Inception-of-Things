apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: app3-route
  namespace: application
spec:
  parentRefs:
  - name: web
    namespace: application
    sectionName: http
  rules:
  - backendRefs:
    - name: app-three
      port: 80
