apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: app3-route
  namespace: kube-system
spec:
  parentRefs:
  - name: web
    namespace: kube-system
    sectionName: http
  rules:
  - backendRefs:
    - name: app3-three
      port: 80
