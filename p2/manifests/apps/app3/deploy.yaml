apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-three
  namespace: kube-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-three
  template:
    metadata:
      labels:
        app: app-three
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html-volume
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html-volume
        configMap:
          name: app3-html
---

apiVersion: v1
kind: Service
metadata:
  name: app-three
  namespace: kube-system
spec:
  selector:
    app: app-three
  ports:
  - port: 80
    targetPort: 80
